<h1>DETAILS page</h1>
  {{#each byId}}
  <div class="moviediv"></div>
      <img src="{{this.image}}" alt="">
      <p>{{this.title}}</p>
    <p>{{this.director}}</p>
  </div>
{{/each}}
 <a href="/movies">Go Movies page</a>
</section>